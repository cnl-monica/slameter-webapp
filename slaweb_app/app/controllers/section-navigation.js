import Ember from 'ember';

/**
 * Controller for group of navigation modules in app's section.
 * This one ony creates default Ember.ArrayController.
 */
var SectionNavigationController = Ember.ArrayController.extend({});

export default SectionNavigationController;
