import Ember from 'ember';

/**
 * Controller for group of main modules in app's section.
 * This one ony creates default Ember.ArrayController.
 */
var WallController = Ember.ArrayController.extend({});

export default WallController;
