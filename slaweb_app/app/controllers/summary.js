import Ember from 'ember';

/**
 * Controller for group of summary modules in app's section.
 * This one ony creates default Ember.ArrayController.
 */
var SummaryController = Ember.ArrayController.extend({});

export default SummaryController;
