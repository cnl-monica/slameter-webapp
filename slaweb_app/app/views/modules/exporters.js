import Ember from 'ember';

var ExporterListView = Ember.View.extend({
    tagName: 'nav',
    classNames: ['sidebar-nav'],
    templateName: 'modules/exporters'

});

export default ExporterListView;
