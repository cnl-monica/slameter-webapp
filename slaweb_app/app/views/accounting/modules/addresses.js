import Ember from 'ember';

var AddressesView = Ember.View.extend({
    tagName: 'nav',
    classNames: ['sidebar-nav'],
    templateName: 'accounting/modules/addresses'

});

export default AddressesView;
