import Ember from 'ember';

var AccUsersListView = Ember.View.extend({
    tagName: 'nav',
    classNames: ['sidebar-nav'],
    templateName: 'accounting/modules/accusers'

});

export default AccUsersListView;
