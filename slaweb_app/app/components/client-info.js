import Ember from 'ember';

/**
 * Component for printing info about a client
 * (see template for this component)
 */
var ClientInfoComponent = Ember.Component.extend({
    classNames: ['client-info']
});

export default ClientInfoComponent;
