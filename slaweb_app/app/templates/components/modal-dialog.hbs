<div class="l-modal-overlay" {{action "cancel"}}>
    <div class="l-modal-dialog" {{action bubbles=false}}>
        {{yield}}
        <div class="modal-controls">
            <button {{action "cancel"}}>Cancel</button>
            <button {{action "ok"}}>OK</button>
        </div>
    </div>
</div>